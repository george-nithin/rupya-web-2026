[phases.setup]
nixPkgs = ["nodePackages.npm", "python311", "nodejs_20"]

[phases.install]
cmds = ["npm install", "python3 -m venv .venv", "source .venv/bin/activate && pip install -r backend/requirements.txt"]

[start]
cmd = "cd backend && python3 main.py"
