[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "nodejs_20"]

[phases.install]
cmds = ["npm install", "python3 -m pip install -r backend/requirements.txt"]

[start]
cmd = "cd backend && python3 main.py"
